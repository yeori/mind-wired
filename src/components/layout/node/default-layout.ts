/**
 * Default layout manager
 * It does nothing, keeping all nodes in their position.
 */

import { type NodeUI } from "../../node/node-ui";

const doLayout = (nodeUI: NodeUI) => {};
const setPosition = () => {};
export default { doLayout, setPosition };
